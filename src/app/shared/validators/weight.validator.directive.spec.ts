import { WeightValidatorDirective } from './weight.validator.directive';

describe('WeightValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new WeightValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
